tasks:
  Run all tasks:
    shell: python -m robot --report NONE --outputdir Outputs Tasks/qa_tester.robot
  
condaConfigFile: conda.yaml
artifactsDir: Outputs
PATH:
  - .
PYTHONPATH:
  - Libraries
  - Variables
  
ignoreFiles:
  - .gitignore
